# MongoDB Atlas - replace <db_password> with your actual password
MONGODB_URI=mongodb+srv://agatenashons_db_user:<db_password>@cluster0.v4yk9ay.mongodb.net/?appName=Cluster0

# Optional: server port (default 4000)
PORT=4000

# Optional: allowed frontend origin (for CORS)
# CLIENT_ORIGIN=http://localhost:3000
